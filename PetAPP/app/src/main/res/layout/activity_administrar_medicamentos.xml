<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="@drawable/gradient_background"
    tools:context=".AdministrarMedicamentos">

    <!-- Logo de la aplicación -->
    <ImageView
        android:id="@+id/logo"
        android:src="@drawable/logo_app"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:contentDescription="@string/logo"
        android:layout_marginBottom="40dp" />

    <!-- Spinner de medicamentos -->
    <Spinner
        android:id="@+id/spinnerMedicamentos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:minHeight="48dp" />

    <!-- Botón para marcar como administrado -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnMarcarAdministrado"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Marcar como Administrado"
        android:backgroundTint="@color/primaryColor"
        android:textColor="@android:color/white"
        android:layout_marginTop="10dp" />

</LinearLayout>
